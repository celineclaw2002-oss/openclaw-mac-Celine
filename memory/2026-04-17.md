- Mission Control Add Agent flow was successfully refactored from fake/local parser chat into a real session-backed Celine authoring flow. Core refactor commit: `06f0301` (`Refactor agent onboarding to real Celine chat`).
- The new onboarding architecture keeps draft/spec/preview entirely in Mission Control UI state until explicit Create, so opening/chatting creates no agent and closing the modal leaves no stray agent.
- New onboarding chat files/path introduced around this refactor: `projects/celine-mission-control/src/app/api/agents/onboard/chat/route.ts` and `projects/celine-mission-control/src/lib/agent-onboarding-chat.ts`, with UI changes in `src/components/panels/agent-onboarding-modal.tsx`.
- Real integration bugs surfaced immediately after the refactor and were fixed in sequence:
  - `b231552` — successful `agent.wait` payloads were being misread as failures.
  - `ab0f3cd` — successful `chat.send` started responses were being misread as failures.
  - `df8263c` — onboarding `agent.wait` timeouts were degraded to non-fatal states instead of raw fatal errors.
- User-reported UX issue remains important context: the onboarding modal auto-bootstraps on open, which causes “Celine is thinking...” before the user types anything. This behavior was made safer, but removing auto-bootstrap entirely is likely the cleaner next UX pass.
- Typecheck completed successfully after the onboarding timeout fix.
- WhatsApp gateway briefly disconnected with status 503 and then reconnected as `+14708125626`; still consistent with earlier transport flakiness rather than the main Mission Control issue.
## 2026-04-17 Mission Control onboarding debugging
- Mission Control Add Agent flow was refactored onto a dedicated long-lived authoring session key `agent:main:onboarding-authoring` using `chat.send` plus transcript JSONL readback instead of relying on `agent:main:main`, unsupported session spawn RPCs, or hanging synchronous `openclaw agent` calls.
- Verified from live session transcript that the authoring runtime *does* produce the correct structured assistant replies for onboarding prompts like "I want to create a research agent called Orm".
- Main remaining failure during user validation: Mission Control UI still showed fallback text like "I'm still thinking about that..." and the draft pane stayed empty even when the correct assistant reply already existed in the transcript.
- Confirmed this is not primarily a model/runtime failure. The likely unstable layer is request-time polling/correlation in the onboarding API route and possibly hot-reload/backend freshness.
- Multiple fixes landed during this debugging cycle:
  - `32cfc95` tolerate CLI close-with-output in command wrapper so Mission Control dev server no longer crashes on `openclaw agents list --json`
  - `2fadbae` correlate onboarding transcript replies to the current prompt
  - `9fa6262` read onboarding reply after matched prompt boundary
- Clean backend restart was forced because hot reload was no longer trusted for the onboarding route.
- Strong next step if issue persists after restart: stop using synchronous request-time waiting for the whole authoring turn, and redesign onboarding as a proper async/session-backed chat flow where POST sends the prompt and the UI or a follow-up fetch reads the completed assistant turn separately.

- Mission Control Add Agent redesign progressed from fragile request-time transcript polling to a persisted async run model.
- Landed commit `b365a9c` (`Redesign onboarding around persisted async runs`): adds `onboarding_runs`, makes POST create pending runs, moves completion into scheduler finalization, and makes GET read persisted DB state instead of waiting.
- Caught an important follow-up bug immediately after: scheduler still had a global 60s tick, so the new onboarding finalizer would not actually run at the intended low latency.
- Landed follow-up commit `411018e` (`Make onboarding finalizer scheduler actually low-latency`) so the 2s onboarding finalizer cadence can work as intended.
- `pnpm typecheck` succeeded after both the redesign and the scheduler fix.
- Current best next step is a single calm end-to-end validation pass of Add Agent in Mission Control, with minimal dev-server churn, then send Can the local Mission Control link once behavior is confirmed.
- User explicitly asked to spend their temporary usage-exhaustion window implementing the two Mission Control follow-up fixes locally: (1) reduce Add Agent runtime slowdown/gateway unavailability without losing the conversational UX, and (2) redesign Trash into a proper confirm flow with comprehensive cleanup.
- During the performance investigation, the most likely remaining load source identified was overlapping onboarding finalization work across three paths at once: POST-triggered background finalize, GET inline finalize, and scheduler finalize. Planned direction: reduce this fan-out while preserving the persisted async run architecture and visible UX.
- Trash hardening scope was reaffirmed: current behavior is still closer to deleting the Mission Control row plus optional workspace deletion than to a true "trash everything related to this agent" flow; backend cleanup semantics need to be strengthened before matching UI confirmation changes.
- Mission Control Workshop sluggishness audit found the primary bottleneck was `/api/agents`, not `/api/tasks`. Measured timings showed `/api/tasks` ~11ms, `/api/projects` ~74ms, `/api/operations/control` ~60ms, while `/api/agents` took ~10.65s locally.
- Root cause of the Workshop slowdown: `src/app/api/agents/route.ts` enriches agents through `attachLifecycleSummaries`, which calls `listRegisteredAgents()` in `src/lib/agent-lifecycle.ts`, which runs `openclaw agents list --json`. That CLI itself took about 9.97s on this machine.
- Landed and pushed a mitigation in Mission Control to cache lifecycle registration lookups longer and dedupe inflight calls, reducing repeated `/api/agents` requests from multi-second waits to single-digit milliseconds after the first hit. Commit: `64b62a0` (`Cache agent lifecycle registration lookups`).
- User-reported Workshop delete crash was traced to a frontend state/URL synchronization bug, not backend deletion failure. After delete, a stale `taskId` remained in the URL while the selected task no longer existed, causing a React `Maximum update depth exceeded` loop in `task-board-panel.tsx`.
- Fixed and pushed the Workshop delete loop by clearing stale task selection from the URL and refreshing state promptly after successful deletion. Commit: `364a5db` (`Fix Workshop task delete selection loop`).
- User preferred removing the temporary-feeling governed pilot fail-safe block from Workshop while preserving the underlying controls. Mission Control was updated so Workshop stays focused on task flow and the same controls now live in Settings in a cleaner form.
- Pushed UI relocation/cleanup for operations controls. Commit: `ff49714` (`Move operations controls into Settings`).
- Mission Control archive view follow-up: user wanted all archive filters/sorts consolidated in one place rather than split between toolbar and archive section. Unified archive controls into the top toolbar and pushed commit `f7c999c` (`Unify Workshop archive controls`).
- Verified New Task behavior conceptually for user: task creation and relay path are functional, but messaging-style tasks only reach the user when the task is routed to a live messaging-capable session (not just any worker agent).
- User tested Mission Control by assigning TASK-012: `Text me "Hi" on whatsapp`. System then issued a quality-review prompt as Aegis, and the completion was approved as adequate.
