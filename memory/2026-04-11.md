
- Mission Control UX/product direction clarified: the interface should feel like chatting directly with Celine, with worker coordination hidden backstage by default. Desired model is user -> Celine -> worker -> Celine -> user, not mixed relay/audit logs in the main thread.
- Implemented and pushed Mission Control conversation-layer UX changes in `projects/celine-mission-control`: commit `ded896c` (`Make task threads feel like talking to Celine`). Changes included making Celine replies more conversational, hiding raw relay comments from the visible task conversation, and rewriting worker return messages so they appear as plain Celine replies rather than explicit `Worker response from ...` wrappers.
- Implemented and pushed agent presence improvements in Mission Control: commit `717961a` (`Show supervisor attachment in agent presence`). Agents roster now considers operations-control attachment state so attached supervisor/worker roles are surfaced more truthfully instead of relying only on stale heartbeat/last_seen status.
- Verified Mission Control data model state: `operations.connected_supervisor_agent = Celine`, `operations.connected_worker_agent = Mission Control Coding Specialist`. DB agent rows showed Celine with `session_key = agent:main:main` and the specialist with a subagent session key.
- Confirmed Celine and Mission Control Coding Specialist are genuinely separate OpenClaw agents. Celine corresponds to the `main` agent under `/Users/jonozgel/.openclaw/agents/main`, while the worker is `mission-control-coding-specialist` under `/Users/jonozgel/.openclaw/agents/mission-control-coding-specialist` with files such as `agent/models.json`, `agent/auth-profiles.json`, and `sessions/sessions.json`.
- Important diagnosis: although code changes were pushed, the live Mission Control UI at `http://127.0.0.1:3414` appeared to be serving a stale frontend bundle. The served compiled output still reflected old code, matching the user screenshot. Root cause is likely that the running Mission Control process/build was not rebuilt or restarted after the latest frontend changes. Next step is to rebuild/restart the local Mission Control app so the pushed UI changes become visible.

- Mission Control real-task validation exposed the core worker-thread bug: dispatching a task to an existing worker session was incorrectly treated as task completion. The placeholder text `Task dispatched to existing session ...` was being written into resolution/comments/review flow instead of waiting for a real worker result.
- Fixed Mission Control dispatch semantics in `projects/celine-mission-control` so existing-session dispatch records the session and keeps the task in progress rather than marking it complete immediately. Committed and pushed as `1b55bca` with message `Do not treat session dispatch as task completion`.
- Real validation task remained `TASK-011 Redesign archived tasks area`, confirming the worker/session path is genuine and the missing piece was completion writeback into the Celine-mediated thread.
- Mission Control Add Agent redesign work continued in `projects/celine-mission-control`.
- User wants Add Agent first window to mirror terminal mental model: dropdown 1 = real provider/auth target, dropdown 2 = model within that provider, then API key and optional OAuth, with local providers like Ollama treated as first-class.
- Confirmed prior Add Agent UI was only wrapping a narrow internal model catalog and did not match terminal runtime/provider flow.
- Inspected repo and found provider/auth logic split across multiple places: `src/components/onboarding/runtime-setup-modal.tsx`, `src/app/api/status/route.ts`, and integrations-related code.
- Identified backend root cause for missing broader OpenAI choices: `/api/status?action=models` returned only static `MODEL_CATALOG` plus live Ollama tags, so OpenAI was not live-discovered at all.
- Add Agent UI was updated to use a broader provider-target flow, merge provider fallback models with discovered models, surface OAuth button beside API key where supported, and treat local/Ollama as first-class.
- Local review app initially appeared unchanged because two servers were present on port 3414; old stale process bound to `127.0.0.1:3414` kept serving old UI. Clean restart required killing both and restarting standalone review app.
- After clean restart, localhost `http://127.0.0.1:3414` served updated Add Agent UI.
- Backend model API was expanded beyond the prior narrow list so live app now shows broader provider/model options, including broader OpenAI family entries such as `openai/gpt-5.4`, `openai/gpt-5.4-mini`, `openai/gpt-5.3-codex`, `openai/gpt-4.1`, `openai/gpt-4.1-mini`, `openai/gpt-4.1-nano`, `openai/o3`, `openai/o4-mini`, and `openai/codex-mini-latest`.
- Important caveat: OpenAI model list is still expanded backend catalog fallback, not true provider-side live discovery yet; Ollama remains the live-discovered provider path.
- Key commits referenced during this pass: `2268add` (`Improve add agent provider and auth selection`) and `5d00275` (`Expand provider model catalog for add agent flow`).
- Likely next best step: refactor `/api/status?action=models` to return provider targets separately from models, add live provider-side model discovery where credentials exist, and keep expanded catalog as fallback only.
