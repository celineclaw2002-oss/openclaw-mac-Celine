- Continued Vault Phase 3 Slice 3.3 planning discussion. Claude proposed section-level / long-note retrieval refinement using ATX heading parsing and `sectionPath` / `sectionSnippet` annotation on primary matches, with `buildContext` promoted to async for bounded file reads.
- Reviewed the plan critically and judged the main concern to be async blast radius, not feasibility. Key stance: the idea is useful long-term and testable, but should be challenged for smallest safe integration rather than rejected outright.
- Drafted a short revision request for Claude: keep FTS5 schema and ranking unchanged, make section annotation best-effort, minimize and explicitly enumerate every caller affected if `buildContext` becomes async, and avoid opportunistic refactors.
- User asked what Aegis is. Verified from Mission Control code/docs that Aegis is the built-in quality-review gate: tasks in `review` are processed by `runAegisReviews()` in `src/lib/task-dispatch.ts`, which sends a reviewer prompt as agent `aegis`, parses `VERDICT: APPROVED/REJECTED`, writes `quality_reviews`, requeues rejected tasks with comments, and fails after 3 rejections.
- Verified task API enforcement that tasks cannot be moved to `done` without Aegis approval (`src/app/api/tasks/[id]/route.ts`).
- Clarified to user that Aegis is a Mission Control reviewer role/agent, not a separate product they intentionally created; it appears in the Vault plan because max Aegis rejections are already one source for failure-lesson write-back and future reviewer-side context is a plausible later Vault integration.
