
- Can explicitly wants a future post-build Mission Control enhancement on the roadmap: auto-discovery/reconciliation of agents that were manually added via terminal/OpenClaw CLI, without needing to prompt Mission Control manually. This should be remembered as a strong candidate project for the Mission Control Coding Specialist after the current build phase.
- Related future architecture preference: consider agent-specific workspaces for durable specialist agents, but not as an immediate build-phase task. This should come back up proactively later when discussing what to do next after the current Mission Control build stabilizes.

- Mission Control live proof was completed on the authenticated fresh review instance: task message -> in-thread Celine reply -> worker relay -> worker response written back into the same task thread. Root cause of the last-mile failure was that `agent.wait` returning `timeout` caused an early return before transcript fallback; removing that short-circuit allowed transcript fallback to extract the worker reply from the real session transcript and persist it into the task thread with `last_worker_run_id`, `last_worker_reply_at`, and `last_worker_reply` metadata.
- Can wants the next Mission Control build to focus immediately on two areas: (1) a chat-driven Add Agent flow where the chat interface gathers all necessary configuration details conversationally and then Celine fully creates and integrates the sub-agent into Mission Control, and (2) a truthful agent discovery/health model that distinguishes states like discovered, registered, attached, healthy, and governed.
- First implementation pass for that next build was completed in `projects/celine-mission-control`: added conversational onboarding via `src/components/panels/agent-onboarding-modal.tsx`, added creation/integration endpoint `src/app/api/agents/onboard/route.ts`, and added lifecycle/health modeling in `src/lib/agent-lifecycle.ts`. This pass was validated with `pnpm typecheck` and `pnpm build`, and committed as `6e73061` (`Add conversational agent onboarding and lifecycle health`).
