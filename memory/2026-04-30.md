- Reviewed Mission Control Phase 13 follow-up on branch `claude/audit-mission-control-9pxXt` at commit `dc8bc7c`.
- Verified code intent: startup backfill now fills historical `token_usage.agent_name` values so `/api/agents/stats` can attribute old token rows, and `task-thread-loop.ts` now writes `duration_ms` on new `task_outputs` rows using elapsed wall time from function entry.
- Updated local `celine-mission-control` checkout to `dc8bc7c` and relaunched the dev instance on `http://127.0.0.1:3030`.
- Important local observation: this workspace DB currently has `token_usage_total = 0`, so the backfill is logically sound but there was no historical token data here to visibly repopulate. `task_outputs_total = 2` and historical rows still have null duration as expected; new replies will populate duration going forward.
- Important regression/risk noted while booting on this commit: startup logs show `startup dedupe: failed (continuing)` with `SQLITE_ERROR: no such column: workspace_id` from the dedupe query against `settings`. App continues running, but the dedupe block assumes a newer schema than this local DB. Worth hardening with schema-tolerant fallback if older DBs are still in play.

- Reviewed Mission Control commit 9c9c9c6 (avatar persistence + gateway activity mirror + gateway-task token capture).
- Verified targeted new tests pass: agent-sync-preserves-mc-identity, gateway-activity-mirror, usage-extract (21/21).
- Full suite remains at 1916/1918 with the same two unrelated failures observed earlier: src/lib/__tests__/agent-sync.test.ts (EACCES mkdir /Users/jonozgel/.openclaw/workspace) and src/lib/__tests__/operations-control.test.ts default-settings expectation mismatch.
- Assessed 9c9c9c6 as low-risk and checked out locally in celine-mission-control.
