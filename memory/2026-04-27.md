- Mission Control / Vault rollout state:
  - Independently re-audited Claude’s Phase 8 Vault claims on `celine-mission-control` branch `claude/approve-phase-6-slice-AnezD`.
  - Verified local repo updated to commit `9d91420` and local Mission Control dev server restarted successfully.
  - Local app URL is `http://127.0.0.1:3020`.
  - Confirmed code-level presence of new Vault-native MCP surfaces including `mc_vault_capture_note`, `mc_vault_search`, `mc_vault_read`, `mc_vault_knowledge`, `mc_vault_retrieval`, and `mc_vault_family`.
  - Confirmed agent curation tools remain safely off by default unless `MC_MCP_VAULT_CURATION_ENABLED=1` is set.
  - Updated workspace guidance so agents prefer Mission Control Vault as the shared operational memory path when available; changed `SOUL.md` and `USER.md`, then committed guidance change as `062536f`.
  - Created resumable topic summary at `topics/mission-control-vault-integration.md` and committed it as `c34443d`.
  - Live route verification is still blocked from CLI by Mission Control auth; unauthenticated requests hit `/login` or `Unauthorized`. Next useful step is operator-authenticated browser verification while Can is logged in.
- Mission Control Vault runtime hardening:
  - Created and pushed fallback branch `can/fallback-mission-control-vault-wow-2026-04-27` before deeper fixes.
  - Traced the live product gap to a runtime split-brain: task execution could still satisfy “save/write to Vault” via the legacy memory/raw note path instead of the structured Phase 8 artifact path.
  - Implemented a dispatch-layer fix in `celine-mission-control/src/lib/task-dispatch.ts` so Vault-writing tasks now explicitly require `mc_vault_capture_note` and explicitly forbid `mc_write_knowledge_file`, `/api/memory`, and raw `03 Notes/...` drops; also requires honest wording so agents do not claim Vault success unless structured capture actually succeeded.
  - Added regression coverage in `celine-mission-control/src/lib/__tests__/task-dispatch-knowledge-injection.test.ts` to prove Vault-writing tasks get the hard structured-capture guidance and ordinary tasks do not.
  - Updated `celine-mission-control/docs/vault-follow-up-punch-list.md` to document the runtime parity hardening and its scope.
  - Validation after the fix passed for:
    - `pnpm exec vitest run src/lib/__tests__/task-dispatch-knowledge-injection.test.ts src/lib/__tests__/vault-acceptance-checklist.test.ts`
    - `pnpm run test:vault-acceptance`
    - `pnpm exec vitest run src/lib/__tests__/task-dispatch-review-brief.test.ts src/lib/__tests__/task-dispatch-lesson-writeback.test.ts`
    - `pnpm run typecheck`
  - Committed the dispatch hardening as `a26faf9` with message `fix(vault): force structured capture in task dispatch`.
  - Honest remaining gap: this fixes the main Mission Control task-dispatch surface, but authenticated live re-verification is still needed to prove the exact “write learnings in the Vault” workflow now lands as a structured artifact in the running app.
- Mission Control Vault forensic/runtime follow-up:
  - Confirmed authenticated structured Vault capture works end-to-end when Mission Control auth is supplied correctly to `scripts/mc-mcp-server.cjs` via `MC_URL` and a valid `MC_COOKIE` session token. Direct tool invocation of `mc_vault_capture_note` successfully created `11 Learning/debug-vault-capture-direct-test.md` with proper frontmatter including `type: learning`, `canonical: false`, `linked_entity_type: task`, `linked_entity_id: "11"`, `agent_authored: true`, and `agent_actor: admin`.
  - Verified the structured artifact surfaced consistently through on-disk file state, `artifact_links`, `GET /api/vault/artifacts?entity_type=task&entity_id=11`, `GET /api/tasks/11`, and `GET /api/vault/knowledge?entity_type=task&entity_id=11&recent_limit=5`.
  - Proved the remaining live problem is not `/api/vault/notes` or Vault storage itself, but the real task runtime/session/auth/tooling chain.
  - Found and fixed live task-dispatch tool exposure gap by explicitly launching assigned task runs with `tools: { profile: 'coding' }` in `celine-mission-control/src/lib/task-dispatch.ts`; added regression coverage and committed `197a2f8` (`fix(vault): expose coding tools in live task dispatch`).
  - Inspected real OpenClaw session store under `/Users/canozgel-macmini/.openclaw/agents/main/sessions` and proved task `11` was contaminated by stale session lineage: task metadata stored `dispatch_session_id = 0cc93114-c003-4260-b89b-559e54c96d99`, while current registry entry `agent:main:main` pointed to `sessionId = 8efedf0a-e726-4905-85a1-a6583b1cde02`; the old `0cc...jsonl` transcript contained unrelated Alphabet/Google earnings content.
  - Confirmed OpenClaw preserves prior session lineage when reusing a session entry, so fresh assigned tasks inheriting an attached/grounded worker session risk appending into the wrong historical transcript/tool/tooling context.
  - Patched `resolveGroundedTargetSession(...)` in `celine-mission-control/src/lib/task-dispatch.ts` so fresh `assigned` tasks do not reuse ambient grounded worker sessions; added regression coverage and committed `3711c4e` (`fix(vault): avoid stale worker-session reuse on assigned dispatch`).
  - Found a deeper contamination bug: current session registry entry could show `sessionId = 8efedf0a...` while still binding `sessionFile` to the old `0cc...jsonl` transcript. Patched around that from Mission Control by forcing fresh assigned-task dispatches to prepend `/new` so they do not trust contaminated continuation context; committed `5621885` (`fix(vault): force fresh task dispatch sessions`).
  - Found a second bookkeeping bug in Mission Control: `dispatch_session_id` persistence was reading the wrong response shape. Expanded task-dispatch parsing so fresh gateway runs persist `meta.runId` / `meta.agentMeta.sessionId` correctly and prefer the real run id; committed `6396686` (`fix(vault): persist real gateway run ids for task dispatch`).
  - Revalidated the dispatch/session fixes with targeted dispatch tests and `pnpm run typecheck` after each patch.
  - Honest final state at compaction: three real workflow bugs are now fixed from the Mission Control side (stale attached-session reuse, contaminated continuation context, wrong run/session bookkeeping), but the final natural end-to-end browser proof is still pending because the latest live rerun hit provider quota (`You have hit your ChatGPT usage limit (plus plan). Try again in ~254 min.`). The substrate/API/authenticated direct-capture path is proven; the remaining unproven piece is the next real assigned-task run once provider availability returns.
