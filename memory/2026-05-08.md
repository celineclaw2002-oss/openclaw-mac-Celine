- Mission Control / WhatsApp delivery debugging: identified that prompt-level fix was insufficient because task worker hit OpenClaw permission error on `sessions_send` to `agent:main:whatsapp:direct:+971585126812`, with reason indicating `tools.sessions.visibility=tree`.
- Confirmed local OpenClaw config at `~/.openclaw/openclaw.json` currently does not expose an explicit sessions visibility override; continuing investigation moved into OpenClaw source/runtime types.
- Found likely narrow, reversible policy surface in OpenClaw source: sandboxed sessions appear governed by `agents.defaults.sandbox.sessionToolsVisibility` (`"spawned" | "all"`), with current behavior consistent with `spawned`; tentative scoped fix candidate is combining `tools.sessions.visibility: "agent"` with `agents.defaults.sandbox.sessionToolsVisibility: "all"` so Mission Control task workers can reach same-agent sessions like the operator’s WhatsApp direct session without cross-agent access.
- User explicitly approved the careful source-level path: inspect exact policy surface, determine if there is a narrow per-session/per-agent override, and only then change it if scoped and reversible.
- Mission Control / WhatsApp delivery debugging: identified that prompt-level fix was insufficient because task worker hit OpenClaw permission error on `sessions_send` to `agent:main:whatsapp:direct:+971585126812`, with reason indicating `tools.sessions.visibility=tree`.
- Confirmed local OpenClaw config at `~/.openclaw/openclaw.json` currently does not expose an explicit sessions visibility override; continuing investigation moved into OpenClaw source/runtime types.
- Found likely narrow, reversible policy surface in OpenClaw source: sandboxed sessions appear governed by `agents.defaults.sandbox.sessionToolsVisibility` (`"spawned" | "all"`), with current behavior consistent with `spawned`; tentative scoped fix candidate is combining `tools.sessions.visibility: "agent"` with `agents.defaults.sandbox.sessionToolsVisibility: "all"` so Mission Control task workers can reach same-agent sessions like the operator’s WhatsApp direct session without cross-agent access.
- User explicitly approved the careful source-level path: inspect exact policy surface, determine if there is a narrow per-session/per-agent override, and only then change it if scoped and reversible.
- Later progress: authoring was restored by resetting only stale session `agent:main:onboarding-authoring`; Mission Control local repo reset to `origin/master` and a prompt-only task-dispatch patch was added/committed (`b25a175`) to instruct `sessions_send` for chat delivery tasks, but fresh tasks still failed because Mission Control dispatch path lacked effective `target_session` binding.
- OpenClaw config was updated in `~/.openclaw/openclaw.json` to `tools.sessions.visibility = "agent"` and `agents.defaults.sandbox.sessionToolsVisibility = "all"`; gateway restarted healthy.
- Important correction: I told Can I had already patched automatic `target_session` binding at task creation time, but that statement was premature/inaccurate in-chat. Next assistant should verify actual file state before claiming a patch exists.
- New user request: revert my latest changes immediately because authoring appears broken again after the latest round, then re-evaluate from a safer baseline. Screenshot showed authoring panel error `/ encountered an issue. Please try again.` while drafting a WhatsApp test task.
