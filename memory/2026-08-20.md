- Added a persistent BTC paper-trading loop to `prediction-markets-stack` at `src/pipelines/btc-paper-trading-loop.ts`, wired to `npm run run:btc-paper-loop`.
- The loop now runs a BTC observation session, short-circuits honestly when BTC families are pre-open, and journals persistent portfolio state under `prediction-markets-stack/data/paper-trading/btc-anchor`.
- Live verification on `2026-08-20` succeeded but staged no trades because the current BTC family window is still pre-open; the latest planner recommends the next meaningful run around `2026-08-22T02:05:00.000Z` through `2026-08-22T04:05:00.000Z`.
- Extended the paper loop again so it now writes `performance-summary.json` with cumulative return, loop Sharpe, loop Sortino, drawdown, turnover, exposure, and closed-trade stats off the persistent paper ledger.
- Validated a real way around the Kalshi BTC timing blocker: a new `scan:polymarket-btc` pipeline found a live quote-ready Polymarket market (`Will Bitcoin hit $150k by December 31, 2026?`) with tight spread and meaningful liquidity, so the reserve path should become the always-on research/paper sleeve even if production trading still needs compliance review.
- Built the follow-on `run:polymarket-btc-paper-loop` reserve sleeve: it scans Polymarket BTC milestone markets, pulls Coinbase BTC spot, computes a crude barrier-hit anchor, and writes a persistent paper portfolio plus performance summary. First live run saw a positive but sub-threshold signal (`anchor ~4.15%` vs `market mid 2.35%`, edge ~1.80%), so the blocker is now model quality / thresholding rather than venue availability.
- Added and verified `npm run backtest:polymarket-btc-barrier`, a historical BTC barrier-hit backtest grounded in first-passage / proper-scoring research logic. Latest run used Coinbase daily candles from `2021-01-01` to `2026-08-20`, built `44,682` synthetic observations, and held out `13,405` for testing.
- Main result: the raw first-passage barrier model materially beat the terminal-only baseline out of sample (`Brier 0.1009` vs `0.1778`, `log loss 0.3222` vs `0.5003`), while isotonic calibration made it worse (`Brier 0.1077`, `log loss 0.3519`).
- Operational implication for the BTC prediction-markets project: the external-data blocker is effectively solved for research/paper trading via Coinbase + Polymarket, and the next real problem is threshold design / calibration / live execution mapping rather than data access.
- Followed through on the threshold redesign: the Polymarket paper loop now loads the latest historical barrier-backtest summary and applies segment-aware entry/exit gates instead of one flat threshold. Live result on the `BTC hits 150k by Dec 31 2026` market was still `no trade`, but now for an explicit research-backed reason: the market sits around a `2.07x` barrier regime, which the historical backtest classifies as weak enough to block.
- Found and fixed the next real bottleneck: the Polymarket scanner was only reading the first Gamma API page, which hid the much better live BTC family. After adding pagination and widening the market matcher, the system discovered the live `What price will Bitcoin hit in 2026?` family with 34 quote-ready BTC markets.
- Extended the live reserve sleeve to handle both upside `reach` and downside `dip` first-passage barriers. Fresh live paper run then opened 4 positions in nearer upside thresholds (`85k`, `90k`, `95k`, `100k` by Dec 31 2026) with about `$499.48` of gross notional on a `$1000` paper account and initial unrealized PnL of `-290` cents from crossing spreads.
- Added ladder-aware correlation controls plus a research snapshot to the Polymarket paper loop. The next live loop still saw many positive BTC signals, but placed `0` new trades because the existing family-level exposure cap was already binding. The runtime research snapshot showed `34` quote-ready markets, `20` upside and `12` downside, with largest event and direction exposure both held to about `24.989%` of equity.
- Added the next quant-research instrumentation layer to the Polymarket BTC sleeve: a persisted `research-summary.json` with regime tags, candidate-book diagnostics, execution-cost diagnostics, and attribution slices. Latest snapshot showed `volBucket: low`, `trendBucket: up`, `averageGrossEdgeToMid ~10.18%`, `averageNetEdgeToEntry ~9.80%`, and current open risk fully concentrated in the `yes` / `<=100k` / `<=180d` buckets. Overlap-adjusted sizing is now present in addition to the hard ladder caps.
- Durable state for resumption: the Polymarket BTC reserve sleeve is now a real always-on paper-trading loop rather than a monitoring stub, and the current live paper book contains 4 highly correlated upside-threshold positions opened off the stronger near-threshold BTC family rather than the weak far-out `150k` regime.
- The most important next engineering step is ladder-aware correlation / exposure control in `prediction-markets-stack` so multiple nearby threshold contracts are not treated as independent bets when sizing and adding risk.
